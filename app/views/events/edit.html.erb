<div class="event-form col-md-6 col-md-offset-3">
  <% if @event.errors.any? %>
      <p class="bg-danger text-danger">Your event could not be updated. Please check your inputs.</p>
      <div id="error_explanation">
        <span><%= pluralize(@event.errors.count, "error") %> prohibited this article from being saved:</span>
        <ul>
          <% @event.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
  <% end %>
  <%= render 'form' %>
</div>