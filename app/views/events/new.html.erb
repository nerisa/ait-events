<div class="event-form col-md-6 col-md-offset-3">
  <h2>Create an event</h2>
  <% if @event.errors.any? %>
      <p class="bg-danger text-danger">Your event could not be saved. Please check your inputs.</p>
      <div id="error_explanation">
        <span><%= pluralize(@event.errors.count, "error") %> prohibited this article from being saved:</span>
        <ul>
          <% @event.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
  <% end %>
  <%= render partial: 'form' %>
</div>